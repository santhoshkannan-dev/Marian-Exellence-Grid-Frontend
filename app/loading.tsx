import { LoadingScreen } from '@/components/loading';

export default function RootLoading() {
  return (
    <LoadingScreen
      message="Loading Marian Excellence Grid..."
      subtitle="Marian College Kuttikkanam (Autonomous) — Class Excellence Portal"
    />
  );
}
